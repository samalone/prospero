apiVersion: v1
kind: Service
metadata:
  name: prospero
  labels:
    app: prospero
spec:
  ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: 8080
  selector:
    app: prospero
